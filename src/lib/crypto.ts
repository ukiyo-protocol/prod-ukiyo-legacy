// import {AES,enc} from "crypto-ts";
// import { CRYPTO_SECRET_KEY } from "../config/constants";
// const encryptHandler = (data: string) => {
//   return AES.encrypt(CRYPTO_SECRET_KEY!, data);
// };

// const decryptHandler = (data: string) => {
//   let res: any = AES.decrypt(data, CRYPTO_SECRET_KEY!);
//    return JSON.parse(res.toString(enc.Utf8));
// };

// export const CRPTO_TS = {
//   encryptHandler,
//   decryptHandler,
// };
export {};
